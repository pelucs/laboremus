mutation CreateConsultResale ($name: String!, $email: String!, $contact: String!, $address: String!, $message: String!){
  createConsultResale(data: { name: $name, email: $email, contact: $contact, address: $address, message: $message }){
    name
    email
    contact
    address
    message
  }
}